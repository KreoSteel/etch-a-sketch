# Etch-a-Sketch

A small browser drawing app built with HTML, CSS, and JavaScript.

## Features

- Draw by hovering over the grid
- Pick a custom drawing color
- Switch to rainbow mode for random colors
- Clear the board at any time
- Create a new grid size from 2 to 100
